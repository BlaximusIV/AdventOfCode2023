# Check what is the maximum amount of space that a position can take, given what's to
# the left and right of it.
# From those values, check what the number viable spaces there are.
# If all # aren't filled in, then it's not a viable combination. (i.e. it can't be touching a #, has to be over it)
# multiply the number of viable spaces for each position to get the total number of viable combinations.


def main():
    with open("test_input.txt") as f:
        lines = f.readlines()

    

main()
